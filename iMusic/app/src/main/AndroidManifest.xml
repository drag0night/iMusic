<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vanhy.com.imusic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/imusic"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/imusic"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NgheNhacActivity" />
        <activity android:name=".AddSongToNewPlaylistActivity" />
        <activity android:name=".PlaylistDetailActivity" />
        <activity android:name=".CaSiDetailActivity" />
        <activity android:name=".AlbumDetailActivity"></activity>
    </application>

</manifest>